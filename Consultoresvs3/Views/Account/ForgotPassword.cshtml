@model Consultoresvs3.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "¿Olvidó la contraseña?";
}

<h2>@ViewBag.Title.</h2>

<form class="form-horizontal">
    <h4>Especifique su correo electrónico.</h4>
    <hr>
    <div class="validation-summary-valid text-danger" data-valmsg-summary="true">
        <ul>
            <li style="display:none"></li>
        </ul>
    </div>    <div class="form-group">
        <label class="col-md-2 control-label" for="Email">Correo electrónico</label>
        <div class="col-md-10">
            <input class="form-control" data-val="true" data-val-email="El campo Correo electrónico no es una dirección de correo electrónico válida." data-val-required="El campo Correo electrónico es obligatorio." id="correo" name="Email" type="text" required>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <a class="btn btn-primary Ec" data-url="@Url.Content("/Account/Recuperarcontrasena/")">Buscar</a>
        </div>
    </div>
</form>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(document).ready(function () {

               //script para enviar el correo
                    $('.Ec').click(function () {
                        var correo = $('#correo').val();
                        $.ajax({
                            method: 'POST',
                            url: $(this).data('url'),
                            data: { correo: correo}
                        })
                            .done(function (data) {

                            }).fail(function () {
                                alert("error");
                            });
                    });
            });
</script>

}
